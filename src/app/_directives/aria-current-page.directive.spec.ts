import { AriaCurrentPageDirective } from './aria-current-page.directive';

describe('AriaCurrentPageDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaCurrentPageDirective();
    expect(directive).toBeTruthy();
  });
});
